{
  "version": 1,
  "run_id": "make-the-background-blue-mtmm9q75",
  "feature_id": "make-the-background-blue",
  "two_pager": "docs/neuron/two-pagers/make-the-background-blue.md",
  "context_manifest": [],
  "expected_artifacts": [],
  "acceptance_criteria": [
    {
      "id": "AC-1",
      "statement": "Opening `index.html` in a browser shows a blue page background instead of the current purple/violet one",
      "success_condition": "implemented as described and the repo's existing tests still pass"
    },
    {
      "id": "AC-2",
      "statement": "No other visual elements (mouse body, eyes, tail, trail dots, text, animations) change color or behavior",
      "success_condition": "implemented as described and the repo's existing tests still pass"
    },
    {
      "id": "AC-3",
      "statement": "Page still renders and functions correctly (no layout shift, no JS console errors, trail/spin/blink animations still run)",
      "success_condition": "implemented as described and the repo's existing tests still pass"
    },
    {
      "id": "AC-4",
      "statement": "Blue tone has sufficient contrast with the white mouse body and white/text elements (`h1`, `.info`) for readability",
      "success_condition": "implemented as described and the repo's existing tests still pass"
    }
  ],
  "acceptance_verification_map": [],
  "assumptions_open": [],
  "runtime_hints": {
    "composition_strategy": "freeform-model-pool",
    "max_wall_hours": 1,
    "max_token_budget_usd": 8
  }
}